\chapter{问题重述与分析}

\section{问题背景}

边坡失稳通常经历缓慢蠕变、加速变形和快速滑移三个阶段。实际监测中，传感器尺度差异、降雨入渗、孔隙水压力变化、微震活动和爆破扰动会共同作用于位移曲线，使“识别阶段、解释扰动、提前预警”不能只依赖单一指标。C题给出5组相关附件，要求依次完成位移校准、阶段识别、异常处理、实验集预测和预警变量筛选。

本文不以单独提高某一问的拟合精度为目标，而是建立一条可复核的证据链：量测校准消除传感器尺度差异，阶段识别给出后续预测和预警的结构基准，异常补齐保证扰动变量可信，阶段预测体现变形规律一致性，预警机制最终回到工程风险处置。

\section{问题要求}

问题一要求将两类位移传感器数据统一到同一量测基准。由于传感器响应大体线性，但局部点可能受噪声影响，校准模型应稳健且不宜过度弯曲。

问题二要求识别边坡变形三阶段。累计位移具有单调趋势，但阶段转换更多体现为速度水平和加速度趋势的变化，因此需要区分“趋势开始改变”和“速度显著跃迁”。

问题三要求处理多源监测数据的缺失和异常，并分析各变量对表面位移的影响。降雨量、微震事件数是稀疏驱动量，孔压和位移是连续状态量，二者不能使用完全相同的异常规则。

问题四要求利用训练集规律预测实验集指定时刻的表面位移。题目给出实验集阶段标签，说明阶段内相对演化位置比绝对日历时间更重要，预测模型必须保持阶段规律一致和位移整体单调。

问题五要求从6个候选变量中选择5个并建立预警机制。预警不能只给出一个分位数阈值，还应说明触发持续时间、多源扰动条件和快速阶段提前量。

\section{各问题之间的逻辑关系}

五个问题并非相互独立。问题一的校准结果统一位移量纲，为问题二的速度计算和阶段识别提供可靠输入；问题二得到的阶段节点进入问题四的阶段归一化时间，并为问题五的速度阈值解释提供依据；问题三补齐后的扰动变量和贡献排序进入问题四残差模型，也为问题五变量筛选提供参考；问题四的预测框架检验阶段规律能否迁移到实验集；问题五则把前述预测和解释结果转化为预警闭环。

\begin{figure}[H]
\centering
\includegraphics[width=0.96\textwidth]{paper_problem_relationship.png}
\caption{各问题关系图（AI辅助润色绘制，见参考文献\protect\citep{openai_chatgpt_image2_2026}）}
\label{fig:problem_relationship}
\end{figure}

图\ref{fig:problem_relationship}体现了本文的递进逻辑：数据校准是量测基础，阶段识别是结构基础，异常处理和变量贡献是扰动基础，阶段预测是规律迁移，变量筛选和预警机制是工程输出。该关系决定了后文不能按五个孤立小题堆叠结果，而必须在每一问结尾说明其结果如何服务下一问。

\section{总体建模思路}

本文采用“鲁棒校准--复合证据分段--变量专属异常识别--阶段归一化残差预测--速度预警闭环”的路线：

\begin{enumerate}
  \item 用Huber稳健仿射模型解决传感器尺度统一问题，并用多模型交叉验证确认一次模型足够稳定。
  \item 用Hampel滤波和滚动中位速度识别速度跃迁，再引入位移趋势和持续加速度作为提前证据。
  \item 对连续状态量使用状态空间补齐，对稀疏驱动量保留真实事件强度，避免把降雨和微震误删为坏点。
  \item 对实验集先生成阶段归一化基线，再用扰动残差模型修正局部偏差，保证预测曲线符合边坡变形规律。
  \item 对预警变量进行全组合验证，并以6 h平滑速度、逆速度趋势和扰动增强共同构成三级预警机制。
\end{enumerate}

\begin{figure}[H]
\centering
\includegraphics[width=0.96\textwidth]{paper_overall_model_route.png}
\caption{本文总体建模路线图（AI辅助润色绘制，见参考文献\protect\citep{openai_chatgpt_image2_2026}）}
\label{fig:overall_route}
\end{figure}

图\ref{fig:overall_route}强调两个层次的闭环：一是题面闭环，即每个附件都对应明确的模型和输出表；二是机理闭环，即阶段识别结果进入后续预测与预警，异常识别结果进入变量贡献和扰动残差解释。物理规律由阶段基线和速度阈值承担，机器学习模型只承担残差修正和贡献排序，从而降低黑箱模型难以解释的风险。

\section{问题--模型--输出对应关系}

表\ref{tab:problem_model_output}列出五个问题的核心数学对象、主模型、求解方法、输出结果和验证方式。后续章节均围绕该表展开：先说明为什么选择该数学对象，再给出模型式和参数估计方法，最后报告输出结果与验证指标。

\begin{table}[H]
\centering
\scriptsize
\caption{问题--模型--输出对应关系}
\label{tab:problem_model_output}
\begin{tabularx}{0.99\textwidth}{L{0.8cm}X X X X X}
\toprule
问题 & 核心数学对象 & 主模型 & 求解方法 & 输出结果 & 验证方式 \\
\midrule
1 & 两传感器同步观测$(x_i,y_i)$ & Huber稳健仿射回归 & IRLS迭代加权最小二乘 & 5个校正值及95\%区间 & 时间块CV、多模型对照、bootstrap \\
2 & 位移$s_t$、速度$v_t$、加速度趋势 & 复合证据变点模型 & Hampel滤波、两断点搜索、加权中位融合 & 两个阶段节点、阶段模型参数 & 分段误差、平均速度、窗口敏感性 \\
3 & 多源变量缺失、异常和位移响应 & 状态空间补齐+GBDT贡献分析 & 变量分类、残差阈值、置换重要性 & 异常表、共同异常表、变量贡献 & 缺失复核、阈值敏感性、模型对照 \\
4 & 阶段标签、归一化时间$\tau$和扰动残差 & 单调PCHIP基线+分阶段GBDT残差 & 阶段归一化、残差收缩、单调投影 & 表4.1预测值和预测区间 & 时间块CV、消融实验、单调性检查 \\
5 & 六个候选变量和速度预警指标 & 全组合验证+三级速度阈值+逆速度 & 六选五枚举、速度积分、持续触发 & 最优五变量组合、阈值和预警时间线 & 双模型排序、窗口敏感性、快速阶段一致性 \\
\bottomrule
\end{tabularx}
\end{table}
